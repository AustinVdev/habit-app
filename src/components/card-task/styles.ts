import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  sectionCount: {
    display: 'flex',
    flexDirection: 'row',
  },
  itemCount: {
    marginHorizontal: 3,
  }
});

export default styles;
