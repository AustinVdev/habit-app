import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  card: {
    width: '100%',
    height: 100,
    padding: 10,
    marginVertical: 10,
    borderRadius: 10,
  },
});

export default styles;
