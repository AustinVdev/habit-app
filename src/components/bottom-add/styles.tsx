import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  button: {
   width: 50,
   height: 50,
   borderRadius: 25,
   position: 'absolute',
   bottom: 15,
   right: 15,
   zIndex: 9999,
  },
});

export default styles;
